# Yolo Game — Python inference subprocess
# Apple Silicon Mac only (MLX). Use Python 3.10–3.12 for the embedded bundle build.
#
# From source:
#   python3.12 -m venv .venv && source .venv/bin/activate
#   pip install -r inference/requirements.txt
#
# Standalone .app: `bash scripts/prepare_bundle.sh` before `npm run dist`.

yolo-mlx @ git+https://github.com/thewebAI/yolo-mlx.git@main
numpy>=1.24.0,<3
